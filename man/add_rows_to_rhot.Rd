% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_table_helpers.R
\name{add_rows_to_rhot}
\alias{add_rows_to_rhot}
\title{Add rows to a rhandsontable-compatible data frame}
\usage{
add_rows_to_rhot(data, n = 1, at = NULL)
}
\arguments{
\item{data}{A data frame compatible with rhandsontable.}

\item{n}{Integer. Number of new rows to add (default = 1).}

\item{at}{Integer. Optional row index position at which to insert the new rows.
If NULL or greater than nrow(data), rows are appended at the end.
If <= 0, rows are prepended at the beginning.}
}
\value{
A data frame with the new rows added.
}
\description{
Safely adds a specified number of rows to a data frame backing a rhandsontable.
Rows can be added at a specific index or appended to the end (default).
New rows are initialized with appropriate default values based on column types.
}
\examples{
\dontrun{
df <- data.frame(ID = 1:3, Name = c("A", "B", "C"))
add_rows_to_rhot(df, n = 2)
}

}
\keyword{internal}
